/**
 * Placeholder spec so Jest has at least one test.
 * Remove or replace once you add Phase 1–4 specs.
 */
describe('Ionic Vue Unit Test setup', () => {
  afterEach(() => {
    jest.clearAllMocks();
  });

  it('runs Jest successfully', () => {
    expect(true).toBe(true);
  });
});
