@.cursor-blueprint.md

Create `LoginPage.vue` in `src/views` using Ionic components (ion-input, ion-button). Use the validators from utils. Dispatch `auth/loginUser` on click. Show ion-loading during request. Then create its test file. Mock the Vuex store and ion-loading controller. Test that the login action is only called if validation passes.
