@.cursor-blueprint.md

Create a file `validators.js` in `src/utils` with functions to validate email and password (password min 6 chars). Then create a test file in `tests/unit/utils/validators.spec.js` with 100% coverage including edge cases.
